[package]
name = "gearbox-planet"
description = "A GPU-driven CDLOD cube-sphere planet: the globe under the sites, refined to where the camera is."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "gearbox_planet"
path = "src/lib.rs"

[dependencies]
bevy.workspace = true
big_space.workspace = true
bytemuck = { version = "1", features = ["derive"] }
